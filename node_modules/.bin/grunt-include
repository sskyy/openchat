#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-include/bin/grunt-include" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-include/bin/grunt-include" "$@"
  ret=$?
fi
exit $ret
